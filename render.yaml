services:
  - type: web
    name: ai-physiotherapy-yoga-studio
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --chdir yoga_app app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
